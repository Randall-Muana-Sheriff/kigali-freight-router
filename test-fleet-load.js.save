 } = require('socket.io-client');

console.log('Initializing Live Fleet Operational Load Test...');

const socket = io('http://localhost:5000', {
  auth: { token: 'Bearer mock-dispatcher-jwt-token-string' },
  transports: ['websocket']
});

// Paths specifically designed to cross directly into the 30.060 hub perimeter
const routes = {
  'Jean Bosco (Maniac Trucking)': [
    { lat: -1.9452, lng: 30.0605 }, // TICK 1: INSTANT BREACH (Inside the 600m red circle!)
    { lat: -1.9450, lng: 30.0601 }, // TICK 2: Staying inside
    { lat: -1.9448, lng: 30.0598 }  // TICK 3: Staying inside
  ],
  'Aline Mutoni (Express Rwanda)': [
    { lat: -1.9532, lng: 30.0681 },
    { lat: -1.9536, lng: 30.0675 }
  ],
  'Faustin Karangwa (Nyabugogo Cargo)': [
    { lat: -1.9387, lng: 30.0449 },
    { lat: -1.9381, lng: 30.0447 }
  ]
};

let iteration = 0;

socket.on('connect', () => {
  console.log('Connected to core gateway pipeline.\n
